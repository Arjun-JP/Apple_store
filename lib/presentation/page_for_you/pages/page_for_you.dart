import 'package:flutter/material.dart';

class PageForYou extends StatelessWidget {
  const PageForYou({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text('for You'),
    );
  }
}
